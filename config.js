/* ============================================================
   Konfiguration — hier trägst du deine Supabase-Zugangsdaten ein.
   Beide Werte sind für Client-Anwendungen gedacht und dürfen
   öffentlich im Quelltext stehen (der anon-Key ist kein Geheimnis;
   was er darf, regeln die RLS-Policies in der Datenbank).
   ============================================================ */
window.DYADIQ_CONFIG = {
  // z. B. 'https://abcdefghijklm.supabase.co'
  supabaseUrl: '',

  // der 'anon public' Key aus Project Settings → API
  supabaseAnonKey: '',

  // Code für die Moderationsansicht: diskussion.html?presenter=DEIN-CODE
  presenterCode: 'b146'
};
